# Pinned for reproducible, supply-chain-safe installs.
# All three flagged advisories below are OPEN upstream — the patched releases
# are not on PyPI yet, so these ARE the latest available builds. Re-run
# `pip-audit` periodically and bump the moment the fixes ship.
anthropic==0.109.1
python-dotenv==1.2.1     # GHSA-mf9w-mj56-hr94 — fix 1.2.2 not yet released
elevenlabs==2.52.0

# Security-relevant transitive deps (only exercised on the ElevenLabs voice path).
requests==2.32.5         # GHSA-gc5v-m9x4-r6x2 — fix 2.33.0 not yet released
urllib3==2.6.3           # PYSEC-2026-141 / -142 — fix 2.7.0 not yet released
